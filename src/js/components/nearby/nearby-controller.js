angular
  .module('troveApp')
  .controller('NearbyCtrl', NearbyCtrl);

NearbyCtrl.$inject = ['$timeout', '$window', '$rootScope'];

function NearbyCtrl($timeout, $window, $rootScope) {
  
  var vm = this;
  

}
