angular
  .module('troveApp')
  .controller('uploadCtrl', uploadCtrl);

uploadCtrl.$inject = ['$timeout', '$window', '$rootScope', 'locationService'];

function uploadCtrl($timeout, $window, $rootScope, locationService) {

  var vm = this;



}
